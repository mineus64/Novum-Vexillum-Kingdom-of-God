capital = 677

set_research_slots = 3
set_stability = 0.5
set_war_support = 0.5

if = {
	limit = {
		has_dlc = "Together for Victory"
	}
	set_autonomy = {
		target = HLS
		autonomous_state = autonomy_integrated_puppet
	}
	else = {
		puppet = HLS
	}
}